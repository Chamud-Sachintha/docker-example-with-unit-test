type Student {
    id: ID
    name: String
    age: Int
    address: String
}

type Query {
    getStudent(id: ID!): Student
}

type Mutation {
    registerStudent(name: String!, age: Int!, address: String!): Student
}